# 작업 규칙

## 원본 자산은 절대 덮어쓰지 않는다
사용자가 제공한 이미지·데이터 원본은 **입력이지 작업 파일이 아니다.** 한 번 덮어쓰면 복구할 수 없다.

- 원본 파일명은 `*_src.*` 로 둔다 (예: `logo_wordmark_src.png`). 이 파일들은 **읽기 전용(chmod 444)** 이며 읽기만 한다.
- 가공 결과는 **반드시 다른 이름**으로 저장한다 (예: `logo_wordmark.png`).
- 이미지 처리 스크립트에서 `im.save(입력파일명)` 형태가 나오면 잘못 짠 것이다. 입력 경로와 출력 경로를 다른 변수로 분리해서 쓴다.
- 원본이 필요 없어 보여도 지우거나 줄이지 않는다. 재작업 시 다시 필요해진다.

## 데이터 파이프라인
- 수집(collect_*.py) → 정제(refine_*.py, extract_edtech.py) → 빌드(build_data.py) → data.js
- 태그·제외 규칙의 정본은 `build_data.py` 하나뿐이다. extract_edtech.py는 이 규칙을 그대로 불러다 쓴다 (이중 관리 금지).
- data.js를 배포할 때는 `index.html`의 `data.js?b=날짜` 캐시 파라미터를 반드시 올린다.

# 태그 검증 리포트
tag_review.md는 빌드 때 자동 생성된다. 신규 태그가 실제 제품을 가리키는지 계약명 원문 표본으로 확인한 뒤 커밋하고, 판단 결과(통과/제외)를 커밋 메시지에 적는다.

## 제품명 자동 발굴 (사람 눈에 의존하지 않기)
범주 태그(코스웨어(기타)·SW·플랫폼(제품명 미상) 등)로만 처리된 계약에는 실제 제품명이 묻혀 있다.
`python3 mine_products.py --apply`를 데이터 갱신 때마다 돌린다.
- A등급(에듀집 사전에 있는 이름)은 mined_rules.csv에 자동 반영된다.
- B등급(업체 쏠림)·C등급은 product_candidates.md에서 확인해 판단 후 반영한다.
- 반영 뒤에는 tag_review.md로 오매칭 여부를 검증하고 결과를 커밋 메시지에 남긴다.

## 정정 요청 처리
정정 요청(특히 공급 기업 제보)은 기록을 하나씩 손대지 않는다.
"제품군으로만 남은 저 기록이 우리 제품"이라는 제보는 계약명에서 그 제품을 알아보는 규칙 문제이므로
`python3 add_rule.py --tag "제품명" --pattern "정규식" --by "제보처 날짜"`로 먼저 무엇이 달라지는지 확인하고
(표본·업체 쏠림·다른 제품 태그와의 충돌을 함께 보여준다), 표본이 모두 맞을 때만 `--apply`로
mined_rules.csv에 남긴다. 그다음 build_data.py로 다시 만들고 tag_review.md로 오매칭을 확인해 커밋한다.
계약명에 제품 이름이 아예 없으면 규칙으로 풀 수 없다 — 업체명이 곧 제품명인 경우에만
build_data.py의 VENDOR_RULES에 넣고 근거를 주석으로 남긴다.
