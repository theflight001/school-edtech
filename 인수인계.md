# 인수인계 — school-edtech.kr (2026-09-15 갱신)

새 세션은 이 문서와 `CLAUDE.md`(작업 규칙)를 먼저 읽는다. 사용자 메모리(`MEMORY.md`)도 자동으로 불러와진다.

## 1. 서비스 한 줄
전국 초·중·고가 공공 조달로 산 에듀테크를 학교·제품·지역별로 보여 주는 정적 사이트. Vercel 배포(main에 push하면 자동).
현재 공개 수치: **수록 327,380건 · 기록 보유 학교 12,144곳 · 검색 가능 학교 12,540곳**. 마지막 커밋: 2026-09-17 전북 과거 연도 반영.

## 1-1. 계정·배포·열쇠 (이 컴퓨터에 이미 설정돼 있다)
- **작업 폴더**: `/Users/keollim/Claude/Projects/edtech` — 새 세션도 반드시 이 폴더에서 연다(메모리·CLAUDE.md가 폴더에 묶여 있다).
- **GitHub**: `github.com/theflight001/school-edtech` (main). `gh` CLI가 theflight001 계정으로 로그인돼 있어 push가 된다.
- **Vercel**: GitHub 연동 자동 배포 — main에 push하면 배포된다. 이 컴퓨터에 vercel CLI·`.vercel` 설정은 없다. 도메인·프로젝트 설정은 Vercel 웹 대시보드에서만 바꾼다(사용자 계정).
- **도메인**: https://school-edtech.kr/ — 배포 확인은 `curl -s https://school-edtech.kr/ | grep -o 'b=2026[0-9a-z]*'`로 캐시 파라미터가 바뀌었는지 본다.
- **API 열쇠**: `~/.edtech_env` (나라장터 NARA_KEY·NARA_BID_KEY, 네이버 검색·광고 키). 수집기가 읽는다. 값을 출력하거나 커밋하지 않는다.
- **예약 작업**: `~/Library/LaunchAgents/com.edtech.{five,jne,monthly}.plist`
- **문의 메일**: 사이트 정정 요청·문의는 schooledtech.info@gmail.com

## 2. 구조
- 수집 `collect_*.py` → 정제 `refine_*.py`(`*_refined.csv`) → 빌드 `build_data.py` → `data.js` 등 → `app.js`가 표시
- 태그·제외 규칙의 정본은 `build_data.py` 하나. 에듀집 규칙 `edzip_rules.csv`, 자동 발굴 규칙 `mined_rules.csv`도 빌드가 읽는다.
- 교육청 일괄 도입(학교가 아닌 시도 단위)은 `refine_office_buy.py` → `office_refined.csv`.
- S2B 입찰: `collect_s2b_bid.py` → `s2b_bid_all.csv` → `refine_s2b.py --src s2b_bid_all.csv --out s2b_bid_refined.csv` → 빌드가 출처 'S2B 학교장터(입찰)'로 싣는다. 자료원·수집 명세 전체는 `자료원_현황_리뷰용.md`(외부 리뷰용으로 작성, 9/15).
- **공급사 명부 `vendors.csv`**(2026-09-15): 에듀테크 제조·개발사 52곳의 정식명·표기·제품 태그. 빌드가 기록에 maker(정식명)를 붙이고, '제품 태그' 칸이 있는 회사(제품 하나만 파는 곳)는 계약명에 제품이 없는 기록에 그 태그를 채운다. 회사를 더하려면 이 파일에 한 줄 추가 후 재빌드. 여러 제품을 파는 회사에는 제품 태그를 적지 않는다(업체명으로 제품을 추정하지 않는다는 원칙). 후보 원본은 `공급사_후보.csv`.
- 배포 전 반드시 `index.html`·`app.js`의 `?b=` 캐시 파라미터를 올린다(현재 `20260919a`).
- 빌드는 약 4분. 로컬 확인은 `.claude/launch.json`의 `edtech`(serve_spa.py, 8912포트).

## 3. 사용자가 정한 원칙 (지켜야 함)
- **수집이 빠진 채로 게시하지 않는다.** 데이터 재수집·재정제 판은 한 지역이라도 실패하면 배포 금지(규칙만 바꾼 재빌드는 예외로 배포해 왔다).
- **도서는 에듀테크가 아니다.** 단 전자책·온라인 독서(리딩게이트·밀리의서재 등)는 에듀테크. 본체·키트와 함께 산 교재는 남긴다.
- **협의회·회의비·다과·홍보물·상품은 뺀다.**
- **체험비·특강비·강사료·교육 위탁은 뺀다.** 인공지능 히어로만 예외.
- **방과후 교재비·재료비, 부속품만 산 계약, 유지보수 계약은 남긴다.**
- **교육청 일괄 도입에서 행정·업무용 SW(본청 MS·한컴, 시설과 AutoCAD, 서버 CAL)는 뺀다.**
- 태그 이름: 웨이메이커→**메이저맵**, 스쿨톡 학교전화녹음→**스쿨톡**, 햄스터S→**햄스터로봇**(모델을 가릴 수 없어 모음).
- 공급사 제보·정정 요청은 기록을 하나씩 고치지 않고 규칙(`add_rule.py`)으로 푼다.
- 화면 숫자는 손으로 적지 않고 빌드가 `meta`에 담은 값을 쓴다.
- 사용자는 **짧고 쉬운 설명**을 원한다. 결론 먼저, 세부는 필요할 때만.

## 4. 최근 바뀐 핵심 — 숫자가 움직인 이유
| 변경 | 효과 |
|---|---|
| S2B·나라장터와 시도교육청 계약공개에 **같은 계약이 두 번** 실리던 것 합침(같은 학교·금액·업체·2개월, 결제 창구 이름은 업체로 안 봄). S2B 쪽을 남기고 교육청 줄을 뺀다. 짝이 끝까지 남을 때만 뺀다. | 약 −19,300 |
| 나라장터 **변경 계약**(ctrtNo 같고 ctrtChgOrd만 다름) 합침 | −1,151 |
| **S2B 분할 계약**(같은 제목·다른 업체·금액)이 '완전 중복'으로 지워지던 것 복원 | +20,575 |
| 태그 누락 검사 반영: 사전 미등록 제품(오조봇·네오씽카·비상 옥수수·코스페이시스·블루킷 등), 띄어쓰기·별칭, 판매대행 업체 계약의 '제품 좁히기' 오류 | 태그 수천 건 |
| AIDT 출판사를 계약명·S2B 업체로 특정 | 일반 AIDT 1,187건 → 출판사 |
| 책만 산 계약, AIDT가 사업명일 뿐인 계약(이어폰·상품 등), 체험·강사료 제외 | 수백 건 |
| 검색: '구입아트봉봉'처럼 붙여 쓴 제품명, '클래스 카드'처럼 띄어 쓴 검색어 | app.js |

**9/15 구조검증 반영(d0f8c6f)** — 외부(챗지피티) 구조검증 보고서 11개 항목 중 10개를 코드로 재현해 고쳤다. 원본은 `~/.codex/.chatgpt-projects/…/2026-09-15-구조검증/`.
| 항목 | 고친 것 |
|---|---|
| 직접 접속 시 '기록 없음'(S01) | 첫 화면 자료(2026년~)에 없으면 옛 자료를 받은 뒤 다시 그린다(`loadingOld`) |
| 공백 학교코드(S02) | 설립 예정 103곳의 코드 공백 7자 → 빈 값 |
| 기간 표기(S03) | 자료가 닿는 달(2026.9)까지 적고 `meta.ymPartial`로 '수집 중' 표시 |
| 홈 도입 학교 수 차트(S04) | 모든 제품의 학교 수로 상위 10개 선정(알툴즈 복귀) |
| 공급 기업 색인(S05) | 옛 자료를 붙인 뒤 `buildVendorIndex()`·`buildMakerKey()` 재실행 |
| S2B 금액(S07) | 내용 문구에 금액·업체를 넣어 표에 보인다 |
| 회사 계열별 차트(S08)·수수료(R03) | 학교 수로 세고, 계약금액 합계에서 결제 수수료 제외 |
| 입찰 공고 집계(S09) | `bid` 표시 열. 화면은 `isCounted`(!dup && !bid)로 통계에서 뺀다. 목록엔 남는다 |
| 고정 숫자(S10)·LG전자(S11) | meta.buriedProducts 사용, MAKER·CHANNEL 정규식 /i |
| 학교 지역 충돌 100건(R01) | 서울·경기·강원 재정제(옛 별칭 제거), 계약명 지명 판별에서 학교 이름 속 지명 제외, 후보를 기록의 시도로 먼저 좁힘 → 10건(S2B 시도 칸 문제) |
| `_vendor_of` 정규식 | ')'에서 끊기던 것 고침. 중복 +232(모두 같은 학교·금액·업체 짝) −5 |

**9/15 수집검증 1차(챗지피티) 반영** - 원본은 ~/.codex/.chatgpt-projects/ 아래 2026-09-15-수집검증 폴더. 확인해 고친 것: 1) 전북·강원·경남 수집기의 학교 판별을 '학교로 끝나면(대학교 제외)'로 넓힘(특수학교·각종학교 누락). 전북은 2020~2025 회계연도 수집을 9/15 23:15에 시작(3초 간격, 약 하루). **강원·경남은 이미 '끝냄'으로 적힌 조합이라 특수학교분 재수집이 별도로 필요.** 2) 나라장터 수집기의 '대학' 포함 제외를 대학교·대학으로 끝날 때만으로(부설 초·중·고 84곳). **이미 받은 나라장터 자료에는 빠져 있어 재수집 필요(API 할당량 고려).** 3) extract_edtech.py 종료일 7/31 고정을 오늘 날짜로(8월분 53건 복구). 4) 쪽 상한 도달을 미완료로. 5) 수집현황 빈 달 사유를 '추정·원천 미확인'으로 표기. 6) 'SW'는 낱말로 홀로 설 때만 소프트웨어(소화기·SW-200 모델명 361건 제외). 7) 결제 표기 업체(이웃닷컴_ealimi·KCP·NICE·사이버결제 등)는 창구로. 8) 학교코드 있는 기록의 시도는 NEIS 명단으로(옛 파일럿 자료 10건). 안 고친 것: 교육청 수집기 중복 키에 금액·업체 추가(체크포인트 형식 변경 필요), 회계연도와 계약일 연도(월간 갱신이 해를 넘기면 두 회계연도를 조회하므로 유지).

**9/18 전국검증(챗지피티) 반영** - 원본은 ~/.codex/.chatgpt-projects/ 아래 2026-09-18-national-audit. 판정·조치 표는 바탕화면 `챗지피티_검토_대응표.docx`. 고친 것: F01 빈 학교코드가 명단 사전에 들어가 코드 없는 1,391건이 '재외'로 붙던 회귀(9/15 S02 수정이 원인) - 빈 코드는 사전에 안 넣고 코드 있을 때만 조회. F02 refine_office.py 학교 판별을 '학교로 끝나면(대학교 제외)'로 - 특수·각종학교 기록 5,045건 수록. F03 'SW 수업용 도서(챗GPT 교육혁명 외)'는 책으로(351건 제외). F04 마우스·키보드·충전함 등 주변기기는 기기로. F05 정제 파일이 9/10 이후 갱신 안 돼 있던 것 - 전 시도 재정제(충남 +6,713 등). F09 데이터 안내의 '최소값'·'100만 원 이상은 대체로 잡힘' 삭제, 검색어 수집 한계 명시. 추가로 발견: 제품명이 확인돼도 '운영비'가 들면 '교육 서비스 계약'으로 빠지던 규칙(refine_office·refine_s2b·extract_edtech·refine_nara_bid) 고침 - **재정제 후 다음 빌드에 반영 필요(주중초 클래스팅·코드모스, 금구초 Snorkl 등)**. 강원 특수학교 11곳은 collect_special.py로 기관명 조회해 3,020행 보충(봉대가온·춘천동원은 50쪽 상한에 닿아 미완). 경남 15곳은 서버가 15분씩 멈춰 진행 중. 미착수: F06 조회 기준 날짜(계약 시작일), F07 총건수 대조·반복 쪽, F08 목록 ID≠계약번호, F10 나라장터 날짜 불일치, F11 호환 키트 태그 의미(사용자 판단).

**안 고친 것(설계 선택)**: S06 조건을 주소에 남기기(새 기능 — 필요하면 별도 작업), R02 S2B·나라장터 출처 링크(기록 단위 주소 없음), R04 좌표 없는 7곳 '미개교·휴교' 표기(사용자 결정), R05 운영 점검.
**검증 도구**: `EDTECH_DUP_DUMP=파일 python3 build_data.py`로 중복 판정 기록을 내보내 규칙 변경 전후를 견줄 수 있다. 기록 id는 판마다 밀리므로 (자료원·학교·계약명·금액·월)로 견준다.

## 5. 돌고 있는 것
- **대구(`collect_dge.py`)만 돌고 있다**(자물쇠 `.locks/대구.lock`). 광주는 9/15 00:13 끝나 e79f5f6에 실었다. 강원은 9/14 끝남.
  대구는 월 단위 조회만 되어 검색어 3,977종 × 45개월을 다 물어야 한다. 남은 조합 약 16만, 하루 5,000회 상한이면 **약 33일**.
  사용자 결정(9/15): 기다리지 않고 광주분 먼저 배포. 대구는 계속 받아 다음 갱신에 싣는다. `dge_candidates.csv`가 수정 상태인 것은 그래서다.
  더 빨리 받는 길: 검색어 없이 월별 전체 목록(한 달 약 2.8만 건, 568쪽)을 받아 규칙으로 거른 뒤 맞는 것만 상세를 부르는 방식(약 13일). 아직 안 만들었다.
- **전남은 다른 장소의 맥북에서 이어받기로 함(9/15).** 이 맥북 IP(221.138.91.73)는 여전히 차단. 이관 절차·되돌리기는 `전남_이관.md`(파일 묶음은 바탕화면 `전남_이관.zip`). 겹치지 않게 `com.edtech.jne` 예약은 **내려 두었다**(`launchctl unload`). 되돌릴 때 `.ckpt_전남.json`+`전남_candidates.csv`를 한 쌍으로 덮어쓰고 `refine_office.py 전남` → 빌드.
- 예약: `com.edtech.five`(매일 04시 지역 수집), `com.edtech.monthly`(매월 10일 갱신).

## 6-0. 2026-09-19에 찾은 것 (최우선)
라이브: 커밋 e769eff · 343,318건 · 12,190교 · 캐시 `20260919a`.
1. **서울 고등학교·특수학교 — 9/19 19:47 보충 수집 시작(사용자 지시).** 395곳(고 342), `scratchpad_sen_hq.sh` → `logs/sen_hq.log`, 10초 간격. 끝나면 `refine_office.py 서울` → 빌드 → 배포. 수집기 기본 목록에 000을 넣었다(5363533). 배경: `collect_sen.py`는 교육지원청 코드 001~011만 돈다. 고등학교는 본청 관할 `searchOraCode=000`에 있다(기관명 '고등학교' 게시물 55,607건, 331곳 이상 — 9/19 원천 조회). 000 전체는 부서까지 20만 게시물이라 `searchCondition=school_name&searchKeyword=학교`·`pageUnit=1000`으로 목록을 받을 것(약 60쪽). 지금 서울 고교의 2022.3 이후 기록은 S2B·나라장터뿐이다. 근거: 챗지피티 서울 재검증(`~/.codex/.chatgpt-projects/…/2026-09-19-seoul-recheck/`).
2. **서울 2024-02는 원천 이상.** 202402 게시물이 1월 계약을 다른 ID로 되풀이한다. 수집기 seen 키가 막아 이중 집계는 없다. 복구 불가 — 서울교육청 문의는 사용자가 한다. 안내 표 비고에 적었다.
3. **서울 에듀파인연계 쪽 상한.** `collect_sen_edufine.py`는 400쪽(4,000행)에 닿아도 완료로 적는다. FY2020 '컴퓨터' 원천 5,053행. 상한 닿으면 미완료로 남기게 고치고 잘린 조합만 다시 받을 것. 2022 회계연도 이후 입찰(FY2022 2,336행·FY2024 4,797행)도 이 화면에 있으나 안 받았다.
4. **경기 쪽 상한 잘림은 6칸뿐이었다(9/19 밤 정정).** 266칸 원천 총건수를 재니 2,000행을 넘는 칸은 에듀테크 2024 상·하, 소프트웨어 2020 하, 인공지능 2023 하, 코딩 2023 하·2024 하. 201쪽 이후를 `logs/goe_tail.log`로 이어받음. 원천 대비 61~85%로 보였던 것은 **경기 원천이 같은 계약을 여러 행으로 되풀이해 싣기 때문**('코딩' 2023 상반기 80행 중 고유 57건). 중간 쪽 표본 104행에서 없는 계약 0건. 검증기의 경기 문턱은 0.6. 남는 한계: 중복 키에 금액이 없어 같은 날 같은 이름·다른 금액 계약(약 3%)이 합쳐진다.
5. **verify_collect.py는 9/19 이전엔 늘 거짓 통과였다**(없는 `safe_kw` 호출 오류를 삼킴). 고쳤다. 인천·충북·전남·세종도 한 번 다시 재야 한다(전남은 이관 중이라 제외).
6. 경기 1~2월 미수집(회계연도 창 어긋남)은 고쳐 받았다. 2025회계연도 공백은 원천에 자료가 없는 것 — 한 칸을 계약 단위로 대조해 확인.
7. 판정 대기: '수업용·학생용 컴퓨터/모니터'처럼 에듀테크 단어 없는 일반 기기 구매를 기기로 실을지(지금은 `EDTECH_CTX`에 안 걸려 제외).

## 6. 남은 일 (우선순위 순)
1. ~~공급사(회사) 태그 제안~~ → **끝남(9/15)**. vendors.csv로 제조·개발사 52곳, 단일 제품 회사 29곳 2,350건에 제품 태그. 사용자에게 보여 준 후보 중 유통사 9곳 제외.
2. ~~S2B 입찰 수집~~ → **끝남(9/15, fa7f100)**. `collect_s2b_bid.py`(stmo001.do list04 계약·list03 낙찰, 분기 창, 25초 간격). 첫 수집 76건 → 에듀테크 13건. 낙찰 2026.08~09 창은 서버 장애로 비어 있음 — 월간 갱신이 최근 3개월을 다시 받는다. 월간 갱신 S2B 자물쇠 안에서 엑셀 뒤에 이어 받고 `refine_s2b.py --src s2b_bid_all.csv --out s2b_bid_refined.csv`로 정제.
3. 대구 수집 완료(또는 수집기 재설계) 후 재정제·재빌드. 전남은 막혀 있어 월간 갱신 판은 전남이 풀릴 때까지 보류 원칙.
4. ~~시도별 공개 기준 안내~~ → **끝남(9/16)**. `office_policy.csv`(시도별 화면·안내문·계약방법·비고, 사람이 확인)와 빌드가 센 관찰값(최소 금액·100만 원 미만 비율·첫 연도)을 `meta.officePolicy`로 보내 데이터 안내에 표로 싣고, 지역별 화면에 주의 문구. 안내문 확인 결과: 부산·경남은 메뉴명이 '수의계약(1백만원 이상)', 서울은 '100만원 이상'이라 적혀 있으나 실제 자료엔 소액 46%. 법령(지방계약법 제43조·시행령 제124조)은 조 번호만 확인, **원문은 국가법령정보센터가 스크립트로만 본문을 내려 자동 확인 못 함** — 사람이 한 번 확인해 문구를 다듬을 것.
5. ~~스쿨톡 교원안심번호 합치기~~ → 끝남(9/15, TAG_ALIAS). 스쿨톡 59건·38개교.
6. 태그 누락 검사(`~/Desktop/태그누락_검사근거.zip`)의 '명칭·범위 확인'(4,749쌍)·'관련 지출'(2,300쌍)은 손대지 않았다. 아두이노·엔트리·NotebookLM 등 제품군/무료 도구 판정이 필요.
7. ~~_vendor_of 정규식~~ → 끝남(9/15, d0f8c6f).
8. 남은 지역 충돌 10건은 S2B 자료의 시도 칸이 NEIS와 다른 마이스터고(한국에너지·해양·소방·의료·국방과학). refine_s2b.py의 시도 처리를 볼 것.
9. 공급 기업 목록에 '이웃닷컴_ealimi' 같은 결제 표기가 따로 선다 — VENDOR_CANON에 추가하거나 vnorm 규칙 보강.
10. 대구 미매칭 학교(비봉초·파호초·서촌초·의흥초·월곡초 각 6~24건)는 NEIS 명단에 없다 — 군위 편입 전 폐교로 보이나 확인 안 됨. 대구해올중·고 1건은 명단에 고등학교 항목만 있다.

**9/15에 확인해 끝난 것(문서가 옛 상태였다)**: ALIAS 지역 조건 → 이미 `ALIAS_SCOPED`로 해결(9/13). 가운뎃점 91건 → `_nkey`가 U+00B7·U+30FB를 같게 봐 이화・금란중 코드 7031211에 이미 이어져 있음. 문구 '명단에 없는 N곳' → '특정하지 못한 N곳'으로 고쳐 배포.

## 7. 알아 둘 함정
- `git add`에 없는 파일(`tag_review.md`는 새 태그가 없으면 빌드가 지운다)을 넣으면 add 전체가 실패한다.
- 빌드 규칙을 시험할 때는 build_data.py의 `rows = list(csv.reader(open(SRC` 앞부분만 exec해서 `tags_of` 등을 불러 쓴다(refine_office.py 방식). 책·AIDT 규칙은 그 뒤에 있다.
- 브라우저 확인 시 캐시 파라미터를 안 올리면 옛 app.js가 뜬다.
- 원본 자산(`*_src.*`)은 덮어쓰지 않는다.
- 전남·대전은 과하게 요청해 막힌 전력이 있다 — 요청 간격을 줄이지 말 것.

## 8. 더 찾아볼 곳 (이 문서에 없는 세부가 필요할 때)
- **왜 이렇게 바뀌었나** → `git log` 커밋 메시지. 결정 이유·검증 결과·건수를 모두 적어 두었다. 예: `git log --since=2026-09-12 --format='%h %s%n%b'`
- **사용자 판정 원칙** → 메모리 `judgments-2026-09.md` (자동으로 불러와짐)
- **판정을 받은 워드 파일** → 바탕화면 `전수검사_남은단계_판정요청.docx`(메모에 사용자 답), `제품분류_전수검사.xlsx`, `태그누락_검사근거.zip`
- **이전 세션 대화 원문(마지막 수단)** → `~/.claude/projects/-Users-keollim-Claude-Projects-edtech/ea7e1f80-681a-4519-8218-e901c4172215.jsonl`
  매우 길다. 필요한 낱말로 grep해서 그 부분만 읽는다.
