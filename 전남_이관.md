# 전남교육청 계약공개 수집 — 다른 맥북(다른 IP)에서 이어받기

작성 2026-09-15. 이 문서 하나로 새 노트북에서 전남 수집을 이어받고, 끝나면 원래 맥북으로 되돌릴 수 있게 썼다.
새 노트북에서 Claude Code를 열면 이 문서를 첫 메시지에 붙여 주면 된다.

## 1. 왜 다른 곳에서 받나
- 전남교육청 서버(jge.go.kr)가 2026-09-04부터 원래 맥북의 IP(221.138.91.73)를 막고 있다. 초당 한 번씩 몇 시간을 두드린 탓이다.
  모뎀 재시작으로도 IP가 안 바뀌었고, 9/15 09:25 확인까지 계속 BLOCKED다.
- 그래서 **다른 장소의 IP**에서 이어받는다. 같은 실수를 하지 않게 아래 간격·상한을 지킨다.

## 2. 지금 상태 (원래 맥북 기준, 2026-09-15)
| 항목 | 값 |
|---|---|
| 수집기 | `collect_ice.py --office 전남` (인천·충북·전남 공용 수집기) |
| 검색어 | 기본 검색어 + `edzip_brand_keywords.txt` 3,964줄 → 3,977종 × 회계연도 7개(2020~2026) = 27,839조합 |
| 끝난 조합 | 7,728 (체크포인트 `.ckpt_전남.json`의 done) |
| 남은 조합 | 약 23,900 |
| 받은 후보 | `전남_candidates.csv` 32,655행 (2026-08-31까지 받은 것) |
| 정제 결과(사이트에 실린 것) | `jne_refined.csv` 10,899건 — 이건 옛 판. 수집이 끝나면 다시 만든다 |
| 원래 맥북의 예약 | `com.edtech.jne`(6시간마다 차단 확인·재개)는 **9/15에 내렸다**. 두 기계가 같은 서버를 두드리면 다시 막히고 체크포인트가 갈린다 |

## 3. 새 노트북에 필요한 것
Python 3만 있으면 된다(표준 라이브러리만 쓴다). API 키·`~/.edtech_env` 필요 없다.

**파일 4개** — `전남_이관.zip`에 담았다(AirDrop이나 USB로 옮긴다. 아래 둘은 git에 없다):
1. `collect_ice.py` — 수집기 (git에도 있음: github.com/theflight001/school-edtech)
2. `edzip_brand_keywords.txt` — 검색어 목록 (git에도 있음)
3. `.ckpt_전남.json` — **체크포인트 (git에 없음, 숨김 파일)**. 어디까지 받았는지와 이미 받은 행의 열쇠(seen)가 들어 있다
4. `전남_candidates.csv` — **받은 후보 (git에 없음)**. 수집기가 여기에 이어 붙인다

3·4는 반드시 **한 쌍으로** 옮기고 한 쌍으로 되돌린다. 하나만 옮기면 같은 행을 두 번 받거나 받은 것을 잃는다.

```bash
mkdir -p ~/edtech-jne && cd ~/edtech-jne
unzip ~/Downloads/전남_이관.zip -d .
ls -la            # .ckpt_전남.json 이 보여야 한다(숨김 파일)
```

## 4. 먼저 한 번만 찔러 본다 — 새 IP가 막혀 있지 않은지
요청 한 번이다. OK가 나와야 다음으로 간다.
```bash
cd ~/edtech-jne && python3 - <<'PY'
import importlib.util
spec = importlib.util.spec_from_file_location("ice", "collect_ice.py")
m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
m.URL, m.SYSID, m.MI = m.OFFICES["전남"]
try:
    b = m.fetch("소프트웨어", 1, "2025", "", "")
    print("OK" if b and len(b) > 5000 else "SHORT(응답이 짧다 — 막혔거나 형식이 바뀜)")
except Exception as e:
    print("BLOCKED:", e)
PY
```

## 5. 받기
간격 10초, 하루 상한 5,000회. 이 둘을 줄이지 않는다(전남·대전이 그렇게 막혔다). 상한에 닿으면 체크포인트를 남기고 스스로 끝나므로 다음 날 같은 명령을 다시 친다.
```bash
cd ~/edtech-jne && EDTECH_SPACING=10 EDTECH_MAXREQ=5000 caffeinate -i \
  python3 collect_ice.py --office 전남 \
    --years 2020,2021,2022,2023,2024,2025,2026 \
    --keyword-file edzip_brand_keywords.txt 2>&1 | tee -a jne_collect.log
```
- `caffeinate -i`는 노트북이 잠들어 끊기지 않게 한다. 뚜껑을 덮으면 멈추니 전원에 꽂고 뚜껑을 연 채 둔다.
- 남은 조합이 약 23,900이고 넓은 검색어는 여러 쪽을 넘기므로, 하루 5,000회면 **닷새 안팎**이다. 더 빨리 받고 싶어도 간격은 그대로 두고 `EDTECH_MAXREQ`만 8,000까지 올린다(10초 간격이면 하루 최대 8,640회).
- 검색어 하나가 끝날 때마다 `[검색어 연도] N페이지까지 · 누적 M건 (요청 K회)` 줄이 찍힌다. 넓은 검색어는 15분쯤 조용할 수 있다.

**막힌 신호**: `재시도(<urlopen error timed out>) → 5초 … 20초 … 60초`가 잇따르면 막힌 것이다. 바로 멈추고(Ctrl+C) 최소 하루 쉰다. 간격을 줄여 다시 시도하지 않는다.

**진행률 보기**
```bash
cd ~/edtech-jne && python3 -c "import json;d=json.load(open('.ckpt_전남.json'));print('끝난 조합',len(d['done']),'/ 27839  받은 행',len(d['seen']))"
```

## 6. 끝나면 원래 맥북으로 되돌리기
1. 새 노트북의 `.ckpt_전남.json`과 `전남_candidates.csv` **두 파일을 함께** `/Users/keollim/Claude/Projects/edtech/`에 덮어쓴다(원래 것의 상위 집합이다).
2. 원래 맥북에서:
```bash
cd /Users/keollim/Claude/Projects/edtech && python3 refine_office.py 전남 && python3 build_data.py
```
3. 확인 뒤 캐시 파라미터(`index.html`·`app.js`의 `?b=`)를 올리고 커밋·push하면 Vercel이 배포한다. 절차는 `인수인계.md` 2절.
4. 전남이 다 받아졌으면 "한 지역이라도 빠지면 배포하지 않는다" 원칙에 걸리던 월간 갱신 판도 낼 수 있다(대구는 별도로 진행 중).
5. 원래 맥북의 예약을 되살릴지 정한다. 전남을 다 받았으면 필요 없다. 살리려면:
```bash
launchctl load ~/Library/LaunchAgents/com.edtech.jne.plist
```

## 7. 하지 말 것
- 두 기계에서 동시에 전남을 받지 않는다(원래 맥북의 예약은 내려 두었다. 손으로도 돌리지 않는다).
- 간격 10초·하루 상한을 줄이지 않는다.
- 체크포인트와 후보 CSV를 따로따로 옮기지 않는다.
- 새 노트북에서 정제·빌드·배포까지 하려 들지 않는다 — 저장소·메모리·배포 설정이 원래 맥북에 있다. 새 노트북은 받기만 한다.
